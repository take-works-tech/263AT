# 現在の到達点と運用手順（2026-08-24 時点）

> **この文書は「今どこにいるか」と「何をすれば動くか」だけを書く。**
> 設計の意図は README、検証の記録は `05_backtest_protocol.md` にある。

---

## 0. 一行で言うと

**米国株について、動くシステムが1つできた。指数には勝たない。
3年以上の窓ではマイナスにならなかった。証拠はまだ足りない。**

---

## 1. 確かに言えること / 言えないこと

| 主張 | 根拠 | **信頼度** |
|---|---|---|
| **3年以上の窓でマイナスにならなかった** | 12/12（最悪 +3.2%） | **比較的高い** |
| 指数（SPY）には勝たない | +8.4% 対 +13.7% | **高い** |
| 指数が下げた年には上回った | 3/3（平均 −8.9% 対 −0.5%） | **サンプル3回のみ** |
| 無作為選択には勝つ | +8.4% 対 +0.7% | 中（同じ交絡を共有） |
| リスク調整後わずかに改善 | シャープ 0.99 → 1.02 | **誤差と区別できない** |

**すべての数字に、除去できない2つの上振れ交絡が残る。**

1. **生存者バイアス** — 2012年の残存率 32.6%。廃止銘柄は最初から入らない
2. **カタログ自体のルックアヘッド** — 2012-2026 は設計にとって全部 in-sample

**この2つを除けるのは前向き記録だけ**（§5）。

---

## 2. システムの構成（確定している）

```
ユニバース   SEC 登録 9,631銘柄（価格の中央値 9年）
ゲート       8つ（うち機能しているのは 6つ。E22 と整理銘柄は仮置きのまま）
買いスコア   採用31本 → **実効 4.8本**（非負 ridge、λ は内側検証で選ぶ）
売りルール   **なし**（測定で有害と判明。§4.6）
サイジング   分数 Kelly 0.25 / 1銘柄15% / 業種35% / 投資上限90%
保有         250日（3〜5年ごとの入れ替えが望ましい。§Q1）
銘柄数       20〜30（集中しても期待値は上がらない。§4.7）
執行         翌営業日の始値
```

### ゲートの状態

| | 閾値 | 状態 |
|---|---|---|
| 上場後6ヶ月未満 | — | 機能 |
| 平均売買代金 | 600万円 | 機能（**300万円には緩すぎる**） |
| 出来高ゼロ日 | 1日でも | 機能 |
| 時価総額 | 30億円 | 機能 |
| **最低株価** | US $1 / JP 100円 | **機能**（$1未満は5年中央値 0.15倍） |
| **継続企業の前提（D13）** | 直近18ヶ月 | **機能**（2026-08-24 に埋めた） |
| 監査意見（E22） | — | **仮置き（True 固定）** |
| 整理・監理（N23） | — | **仮置き（False 固定）** |

---

## 3. 動かす手順

### データを作る（初回・年1回）

```
# 価格（1-2時間）
.venv/Scripts/python.exe tools/fetch_prices.py --from-sec --period max

# 財務（SEC DERA、1時間）
.venv/Scripts/python.exe tools/build_pit_fundamentals.py --all

# 継続企業の前提（10分）
.venv/Scripts/python.exe tools/build_gc.py

# 証券種別（1分）※ **これが無いと社債・優先株・ETF が混ざる**
.venv/Scripts/python.exe tools/build_sectypes.py

# パネル（40分）※ **| head で切らない。SIGPIPE で黙って止まる**
.venv/Scripts/python.exe tools/build_panel.py \
    --start 2012-01-31 --end 2026-05-31 --horizons 250 --gates on --rebuild
```

### 測る

```
.venv/Scripts/python.exe tools/run_shrink_wf.py --panel gate --horizon-days 250
.venv/Scripts/python.exe tools/run_system.py    --panel gate --horizon 250     --dump data/eq_gate.json

# **転がり窓。「マイナスにしない」が壊れていないか毎回見る**
.venv/Scripts/python.exe tools/measure_windows.py data/eq_gate.json     --compare data/eq_d13.json
```

### **発注内容を見る（実運用の判断に要る）**

```
.venv/Scripts/python.exe tools/make_orders.py
```

**これを作って初めて、上位3銘柄が社債だと分かった**（docs/10）。
順位とスコアだけ見ていては気づけない。

### **記録する（最重要・年1回）**

```
.venv/Scripts/python.exe tools/record_generation.py            # 記録
.venv/Scripts/python.exe tools/record_generation.py --verify   # 連鎖の検証
.venv/Scripts/python.exe tools/record_generation.py --evaluate # 実現値と突合
```

### 検査

```
python tools/run_tests.py     # 31モジュール + 前向き記録の連鎖
```

---

## 4. **触ってはいけないもの**

| | 理由 |
|---|---|
| `src/prior.py` の `ADOPTED` | **成績を見て変えたら選択になる。** 規則（他者の再現 t>=3.0）だけで決める |
| `docs/07_*.md` `docs/08_*.md` | 事前登録。**測定後の書き換えは `freeze_prereg.py` が検出する** |
| `data/generations.jsonl` | 前向き記録。**1件でも直すと `run_tests.py` が落ちる** |
| カタログの符号 | 推定に任せるとノイズで反転する |

---

## 5. 次にできること（すべて私の判断では進められない）

| | 内容 | 性質 |
|---|---|---|
| **待つ** | 前向き記録の初回評価は **2027-02-05** | **時間だけが解決する** |
| 実運用 | 上の構成で実際に発注する | **ご自身の判断** |
| 日本株 | J-Quants Light ¥1,650/月〜。**独立な検証データ** | 費用の判断 |

**「パラメータを増やす」はこの一覧に無い。**
実効本数が 4.8/27 で飽和しており、
**再現 t が 9 を超える本を2つ足しても ±0.01pp だった**（§4.9）。

---

## 6. 未着手で、価値がありうるもの

**いずれも「やれば良くなる」とは言えない。** 記録として残す。

| | 内容 | 見込み |
|---|---|---|
| E22（監査意見） | EDGAR 全文検索で同様に埋められる | **D13 同様、既に買っていない銘柄を除くだけと予想** |
| 物価（Y03） | 実装済み・予測は的中したが**業種内で一定**で実装と噛み合わない | 業種配分を動かす経路が要る |
| 土地の含み益 | **日本固有・大手が狙わない領域** | EDINET + 国交省の登録が要る |
| 特許 | 公開日（出願日ではない）で数える | PatentsView / EPO の登録が要る |
| LLM の判断 | **後知恵と区別できない** | 匿名化は失敗（4社とも特定された） |

---

## 7. このプロジェクトで繰り返し起きたこと

**8つの「測定を壊す欠陥」のうち6つは、良い成績ではなく
「おかしな数字」から見つかった。**

そして**失敗が成功の顔をして返ってくる**ことが繰り返し起きた。

| | 症状 |
|---|---|
| `min_trade_frac` を上げる | 取引をやめるが「CAGR +0.50%」と表示 |
| `max_names` を下げる | 現金比率が上がるだけだが「集中の効果」に見える |
| `quarter_series` の戻り値 | **自己テストは 17/17 通過**、実データで落ちる |
| `\| head` でパイプを切る | **終了コード 0 で「完了」と報告される** |
| `str.replace` の空振り | **一致しなくてもエラーにならない** |

→ **数字が出たら、まず測り方を疑う。**
→ **「完了」と表示されても、行数を数える。**
